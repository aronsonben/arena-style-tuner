// Tailwind v4 uses CSS-first configuration via @theme in index.css.
// This file is intentionally minimal — theme, colors, fonts, dark mode,
// and animations are all defined in index.css using @theme and @custom-variant.
export default {};